# Grafana 启动时自动创建 Prometheus datasource，读者不需要手动在 UI 中配置。
apiVersion: 1

datasources:
  - name: Prometheus
    uid: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
