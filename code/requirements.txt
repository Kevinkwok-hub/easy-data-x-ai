# D1 课程代码依赖

# LangChain 核心
langchain>=1.2.15
langchain-openai>=1.1.12
langchain-core>=1.2.27,<2

# 环境变量管理
python-dotenv>=1.2.2

# OpenAI 兼容 API 客户端（用于 SiliconFlow Embedding 调用）
openai>=1.0.0

# 向量数据库（可选，用于 d1_5 和 d1_6）
pyseekdb>=1.2.0.post1

# Agent 相关（可选，用于 d1_6）
langgraph>=1.1.6

# X2 Skill 管理（需要 Python 3.11+ 与 pyseekdb）
pyseekdb>=1.2.0.post1
PyYAML>=5.4.1

# X5 MCP 实战
# X5 使用 mcp 1.x 的 FastMCP 导入路径，2.0 已移除该兼容入口。
mcp[cli]>=1.28.1,<2

# D3 RAGAS 评测（d3_5_ragas_eval.py；0.2.x 与当前 Dataset 字段 API 对齐）
ragas>=0.2.0,<0.3
datasets>=2.14.0
